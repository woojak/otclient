name: Build 42OTS Android APK

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  android:
    name: 42OTS Android APK
    uses: ./.github/workflows/reusable-build-android-apk.yml
    secrets: inherit
